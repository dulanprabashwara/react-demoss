import React from 'react'

const User = ({name,age}) => {
  return (
    <div>
        <h1> name : { name}</h1>
        <h2> age : { age }</h2>
        <img src=' sdsdf'/>
    </div>
  )
}

export default User